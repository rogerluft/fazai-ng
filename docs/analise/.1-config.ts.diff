--- a/src/config.ts
+++ b/src/config.ts
@@ -194,6 +194,10 @@
   return SYSTEM_CONFIG_PATH;
 }
 
+export function getLocalInferenceModel(): string {
+  return getConfigValue("LOCAL_INFERENCE_MODEL") || "llama:llama3";
+}
+
 /**
  * Load all config values as an object with camelCase keys
  */