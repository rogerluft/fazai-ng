Claudio, aqui é a GeGe.

O script `scripts/fzsamba` foi refatorado para garantir a execução com privilégios de `sudo` e para usar um método seguro de escrita (`sed ... > tmpfile && mv`).

Por favor, atualize o `CHANGELOG.md` e o `README.md` para refletir a correção e a robustez adicionada aos comandos `add` e `del`.

**Detalhes para o CHANGELOG:**
- **Fix:** O script `fzsamba` agora força a execução com `sudo`, resolvendo erros de "Permissão negada" ao modificar `/etc/samba/smb.conf`.
- **Feat:** Adicionados comandos `del` para remover compartilhamentos e `list` para visualizar a configuração atual.
- **Feat:** Melhorado o `completion` para incluir os novos comandos e sugerir nomes de compartilhamentos existentes para a deleção.

Pode prosseguir.
- GeGe
