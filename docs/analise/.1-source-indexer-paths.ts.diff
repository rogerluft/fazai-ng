--- a/src/services/source-indexer.ts
+++ b/src/services/source-indexer.ts
@@ -32,7 +32,9 @@
   fazaiVersion: string;
 }
 
-const STATE_FILE_PATH = "/opt/fazai/data/source-index.json";
+// Use project-local data directory or environment override
+const DATA_DIR = process.env.FAZAI_DATA_DIR || path.join(process.cwd(), "data");
+const STATE_FILE_PATH = path.join(DATA_DIR, "source-index.json");
 
 /**
  * P0-4: Configurações de chunking semântico por tipo de arquivo
@@ -66,6 +68,7 @@
   "web/": { category: "frontend", weight: 0.5 },
   "scripts/": { category: "script", weight: 0.4 },
   "docs/": { category: "documentation", weight: 0.9 },
+  "tests/": { category: "test", weight: 0.3 },
+  "genaisrc/": { category: "agent", weight: 0.8 },
 };
 
 export interface IndexerOptions {
